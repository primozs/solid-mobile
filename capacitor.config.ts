import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'solid-mobile',
  webDir: 'dist',
  bundledWebRuntime: false,
  plugins: {},
};

export default config;
